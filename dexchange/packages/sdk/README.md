# packages/sdk

DEX SDK와 typed client 자리다.

예상 책임:

- chain config
- contract ABI bindings
- token metadata helpers
- quote and slippage helpers
- API client

UI와 indexer가 같은 domain type을 사용하도록 `packages/shared`와 함께 관리한다.
