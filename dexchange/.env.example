# local placeholder values only. Do not put real private keys or production secrets here.

NODE_ENV=development
APP_ENV=local

DATABASE_URL=postgresql://dexchange:dexchange@localhost:5432/dexchange
REDIS_URL=redis://localhost:6379

LOCAL_CHAIN_RPC_URL=http://127.0.0.1:8545
LOCAL_CHAIN_ID=31337

PUBLIC_APP_URL=http://localhost:3000
PUBLIC_DEFAULT_CHAIN_ID=31337

# Placeholder only. Use local throwaway keys in an untracked .env file if needed.
DEPLOYER_PRIVATE_KEY=
RPC_PROVIDER_API_KEY=
