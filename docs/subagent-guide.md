# 서브 에이전트 가이드

Claude Code의 `Agent` 도구와 Codex의 subagent prompt guide를 사용해 역할별 보조 에이전트를 실행하는 방법과 기준을 정리한다.

## Skills vs 서브 에이전트 역할 구분

같은 본 에이전트 컨텍스트에서 동작하는 `Skills`와, 별도 컨텍스트에서 동작하는 `서브 에이전트`는 책임이 다르다.

### Skills (`.claude/skills/<name>/SKILL.md`)

- 본 에이전트가 직접 실행한다. 컨텍스트가 공유된다.
- 사용자 자연어 발화의 트리거 키워드로 자동 활성화된다.
- 책임: 사용자와의 대화형 요청 구조화(intake, 작업 요청 5종, 라우터).
- 산출물: `templates/*.md` 구조에 맞춘 마크다운 블록.
- 본 에이전트가 직접 사용자에게 응답을 만든다.

### 서브 에이전트 (`Agent` 도구 + `.claude/agents/<name>.md` / `.codex/agents/<name>.md`)

- 별도 컨텍스트에서 독립 실행된다. 본 에이전트의 대화 이력을 보지 않는다.
- 본 에이전트가 명시적으로 호출한다 (자동 활성화 아님).
- 책임: 탐색/조사/리뷰/계획 등 본 에이전트 컨텍스트를 보호해야 하는 작업.
- 산출물: 본 에이전트가 받아서 검토 후 사용자에게 요약 전달.

### 결정 기준

- 사용자와의 대화로 정보를 수집하거나 요청을 구조화해야 하면 -> Skill.
- 대량 파일 탐색, 독립적 리뷰, 사전 계획 수립 등 본 컨텍스트와 분리해야 할 작업이면 -> 서브 에이전트.
- 두 메커니즘은 충돌하지 않는다. 예: `feature` skill이 요청 구조화를 마친 뒤, 본 에이전트가 구현 전 `planner` 서브 에이전트를 호출할 수 있다.

## 서브 에이전트란

Claude Code는 `Agent` 도구를 통해 독립적인 서브 에이전트를 실행할 수 있다. Codex에서는 `.codex/agents/*.md`를 subagent 프롬프트 기준으로 사용해 같은 역할을 수행한다.
서브 에이전트는 별도의 컨텍스트에서 동작하며, 본 에이전트의 컨텍스트를 공유하지 않는다.
따라서 프롬프트에 필요한 배경, 목적, 제약을 명확히 담아야 한다.

## 언제 서브 에이전트를 쓰는가

- 독립적으로 수행 가능한 작업이 병렬로 존재할 때
- 본 에이전트의 컨텍스트 창을 보호해야 할 정도로 결과가 대량일 때
- 역할이 명확히 분리되어 있고, 단계별 검토가 필요할 때

## 언제 서브 에이전트를 쓰지 않는가

- 단순하고 범위가 명확한 작업은 직접 처리한다.
- 서브 에이전트가 이미 수행한 조사를 본 에이전트가 중복으로 수행하지 않는다.
- 결과를 즉시 참조해야 하는 순차 작업은 직접 수행한다.

## 역할별 서브 에이전트

이 저장소의 역할 분리(`agents/`)는 Claude Code 서브 에이전트 타입 및 Codex prompt guide와 다음과 같이 대응한다.

### general-purpose (기본 범용 에이전트)

- 복잡한 멀티스텝 작업, 코드 검색, 파일 탐색에 사용한다.
- 역할 문서: `agents/main-agent.md`
- 사용 시점: 요청 해석이 필요하거나 전체 작업 흐름을 조율해야 할 때

### Explore (탐색 전용 에이전트)

- 파일 패턴 검색, 키워드 검색, 코드베이스 구조 파악에 특화되어 있다.
- 역할 문서: `agents/researcher-agent.md`
- 사용 시점: 기존 구조 조사, 중복 파일 확인, 근거 수집이 필요할 때
- 탐색 수준: `quick` / `medium` / `very thorough` 중 명시한다.

### Plan (설계 전용 에이전트)

- 구현 계획 수립, 핵심 파일 식별, 아키텍처 검토에 사용한다.
- 역할 문서: `agents/main-agent.md` + `agents/executor-agent.md`
- 사용 시점: 큰 기능 추가나 구조 변경 전 구현 전략을 먼저 정리할 때

## 프롬프트 작성 기준

서브 에이전트는 본 에이전트의 대화 이력을 볼 수 없다.
프롬프트에는 다음을 반드시 포함한다.

- 목적: 무엇을 달성하려 하는가
- 배경: 이미 확인한 것, 제외한 것
- 산출물: 어떤 형태로 결과를 돌려줘야 하는가
- 제약: 수정 금지 파일, 범위 제한, 응답 길이 등

## 병렬 실행 기준

독립적인 작업은 단일 메시지 안에 여러 `Agent` 호출을 묶어 병렬로 실행한다.
순서가 중요하거나 앞 결과가 뒤 작업에 필요한 경우에는 순차 실행한다.

예시:
- 병렬 가능: 두 개의 독립된 파일 조사, 관련 없는 두 영역의 탐색
- 순차 필요: 조사 결과를 반영해야 하는 구현, 구현 후 리뷰

## 결과 처리

- 서브 에이전트의 결과는 본 에이전트가 검토한 뒤 사용자에게 전달한다.
- 서브 에이전트 결과를 그대로 노출하지 않고, 핵심만 요약해서 전달한다.
- 서브 에이전트가 작성한 내용 중 의심스러운 부분은 직접 확인 후 사용한다.

## 서브 에이전트 템플릿 디스패치

Claude에서 본 에이전트가 Agent 도구를 호출할 때 `.claude/agents/`의 템플릿을 읽어 프롬프트에 포함한다. Codex에서 subagent를 사용할 때는 같은 역할의 `.codex/agents/` 문서를 읽어 프롬프트 기준으로 삼는다.

### 디스패치 기준

- 조사/탐색 작업 -> Claude: `.claude/agents/explorer.md` (Explore 타입) / Codex: `.codex/agents/explorer.md`
  - 트리거: 기존 구조 파악, 중복 확인, 근거 수집, 영향 범위 조사
  - 병렬 가능: 독립된 두 영역을 동시에 탐색할 때

- 코드 리뷰 작업 -> Claude: `.claude/agents/code-reviewer.md` (general-purpose 타입) / Codex: `.codex/agents/code-reviewer.md`
  - 트리거: 구현 완료 후 리스크 점검, PR 리뷰, 회귀 확인, repo conventions 검토
  - 순차 필요: 구현이 끝난 뒤에만 의미 있음

- 설계/계획 작업 -> Claude: `.claude/agents/planner.md` (Plan 타입) / Codex: `.codex/agents/planner.md`
  - 트리거: 큰 기능 추가 전, 구조 변경 전, 복합 요청 분해 시
  - 순차 필요: 조사 결과가 있어야 정확한 계획 가능

- 테스트 실행 작업 -> Claude: `.claude/agents/test-runner.md` (general-purpose 타입) / Codex: `.codex/agents/test-runner.md`
  - 트리거: 구현 완료 후 테스트 실행, 테스트 실패 분석, 회귀 테스트
  - 병렬 가능: 코드 리뷰와 동시에 실행 가능

- 기능 구현 작업 -> Claude: `.claude/agents/feature-dev.md` (general-purpose 타입) / Codex: `.codex/agents/feature-dev.md`
  - 트리거: 설계 완료 후 end-to-end 기능 구현 위임
  - 순차 필요: planner 결과가 있어야 정확한 구현 가능

- 디자인 일관성 리뷰 -> Claude: `.claude/agents/design-reviewer.md` (general-purpose 타입) / Codex: `.codex/agents/design-reviewer.md`
  - 트리거: UI 산출물 검토, `DESIGN.md` 토큰 호출 준수 검증, Do/Don't 위반 검출, 다크 모드 alias 누락 점검
  - 병렬 가능: `code-reviewer`와 직교(코드 품질과 디자인 일관성은 별도 축)이므로 동시에 실행 가능
  - 순차 필요: UI 산출물이 만들어진 뒤에 의미 있음

### 직접 처리 (서브 에이전트 불필요)

- 단순하고 범위가 명확한 작업
- 결과를 즉시 참조해야 하는 순차 작업
- 서브 에이전트가 이미 수행한 조사와 동일한 범위

## 역할 문서 참조

- **[총괄 진행](../agents/main-agent.md)** - 요청 해석, 범위 통제, 우선순위 판단
- **[구현 작업](../agents/executor-agent.md)** - 코드 수정, 문서 작성, 설정 변경
- **[조사 작업](../agents/researcher-agent.md)** - 구조 탐색, 중복 확인, 근거 수집
- **[리뷰 작업](../agents/reviewer-agent.md)** - 리스크 점검, 회귀 확인, 테스트 누락 검토
