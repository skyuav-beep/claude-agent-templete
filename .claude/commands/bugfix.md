---
description: "버그 수정 요청을 재현 방법·기대 동작·영향 범위로 구조화한다"
argument-hint: "[버그 현상]"
---

# 버그 수정 요청 구조화

버그 수정 요청을 대화형으로 구조화한다.

> 경로 규칙: 아래 `templates/`·`agents/`·`docs/` 경로는 프로젝트 루트 기준이다. 프로젝트에 해당 파일이 없으면 `rules/` 아래 같은 경로를 읽는다(공통 템플릿을 `rules/` symlink로 연결한 프로젝트).

## 실행 방법

1. `templates/bugfix-request.md` 파일을 읽는다.
2. 템플릿의 섹션(문제 요약, 재현 방법, 기대 동작, 영향 범위, 검증 기준)을 순서대로 질문한다.
3. `$ARGUMENTS`가 있으면 사용자 입력에서 버그 현상과 재현 정보를 먼저 파싱해 채운다.
4. 모든 섹션이 채워지면 완성된 bugfix-request 마크다운 블록을 출력한다.

## 인수 처리

- `$ARGUMENTS`가 있으면 버그 현상 설명으로 간주하고 문제 요약을 미리 채운다.
- `$ARGUMENTS`가 비어 있으면 "어떤 문제가 발생하는지" 질문으로 시작한다.

## 진행 규칙

- 재현 방법은 환경, 동작, 결과를 순서대로 수집한다.
- 사용자가 재현 방법을 모르면 관찰된 증상만으로 진행한다.
- 검증 기준은 반드시 수집한다.

## 완료 후

구조화된 bugfix-request 마크다운 블록을 1단계 산출물로 출력하고 `docs/approval-workflow.md`의 2단계부터 진행한다.
